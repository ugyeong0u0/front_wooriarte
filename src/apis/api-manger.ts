import LoginDTO from "./request/login/LoginDTO";

const DOMAIN ='http//' //  TODO("도메인 부족 ")

const SIGNIN_URL = ()=> `${DOMAIN}/login/business`;

export const signInRequest =async(requestBody : LoginDTO)=>{
    
}
export const signinResponse =()=>{  // TODO("반횐값 DTOclass 없음 작성해야함")

}
